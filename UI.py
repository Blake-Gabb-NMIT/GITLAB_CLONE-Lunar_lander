#this is meant to handle all functions that will effect the interface, like fuel, mono ETC
#not sure how pygame reacts to functions from imports, but one way to find out
#this file isnt in use, just yet but who knows
import pygame


def fuelCounter(screen,fuel):
    font = pygame.font.Font(None,54)
    font.render(str(fuel), True, font_color)
